@AGENTS.md
@BUILD_GUIDE.md

# Lumora Treks — Frontend (this repo)

This repo is the **FRONTEND ONLY** (Next.js). The Django + Wagtail CMS lives in a **separate repo** — never scaffold or build it here.

**Read `BUILD_GUIDE.md` (imported above) first.** It is the authoritative guide: the workflow for building any page from a Figma link, the stack/conventions, the exact design tokens + fonts, and the **reusable-components registry**. Keep the registry in `BUILD_GUIDE.md` up to date whenever you add a reusable component.

Page specs: `PLAN.md` / `FRONTEND_PLAN.md` (architecture) · `PACKAGES_PAGE_PLAN.md` (packages page).
